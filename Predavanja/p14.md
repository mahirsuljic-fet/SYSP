Problem sa API kojeg smo radili do sada je sto nemamo nacin kako da funkcija koja se izvrsava na drugom threadu vrati vrijednost koju neki drugi thread moze iskoristiti.

CPU bound \
IO bound

sinhrono i asihrono izvrsavanje

`std::async`

launch policy

`std::future` - tip koji sadrzi buduci rezultat izvrsenja `std::async` funkcije

Kako bi dobili stvarnu vrijednost iz `std::future` koristimo metod `get()`.

future ima cv mutex bool T i thread

`get` blokira ako vrijednost nije spremna, kada bude spremna `future` radi `notify_all` i vraca vrijednost


thread pool - skup niti koji se unaprijed alociraju \
alocira se onoliko niti koliko ima jezgri na racunaru \
koristi se tipicno za CPU bound taskove

prednost je sto ne moramo svaki puta alocirati novu nit, oslobadja se tog overheada

actor ima queue poruka kojeg mu se prosljeduju
ima i listu lambda izlaza koji imaju proizvoljne inpute i outpute
princip je da actor uzme poruku, prodje kroz lambde, pronadje lambdu koja je moze izvrsiti (spremna i po tipu) i izvrsi je

implementira se pomocu thread pool-a

Zadaca: implementirati thread pool ss `std::future` koristeci variadicne template
